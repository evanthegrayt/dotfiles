# vi: set ft=sh :

if [[ $USER == 'vagrant' ]]; then
    CDC_DIRS=($HOME/workflow /vagrant/src)
else
    CDC_DIRS=($HOME/workflow $HOME/.oh-my-zsh/custom/plugins
        $HOME/workflow/vagrant-ofa/src)
fi

CDC_IGNORE=(training hold save)

